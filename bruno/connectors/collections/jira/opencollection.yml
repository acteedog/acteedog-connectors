opencollection: 1.0.0

info:
  name: jira
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth:
    type: basic
    username: "{{email}}"
    password: "{{token}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
