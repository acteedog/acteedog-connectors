info:
  name: Test Connection
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/rest/api/3/myself"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
