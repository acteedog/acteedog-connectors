info:
  name: Fetch Issue
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/rest/api/3/issue/{{issue_id}}"
  auth: inherit

runtime:
  variables:
    - name: date
      value: 2026-03-10
    - name: issue_id
      value: "10038"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
