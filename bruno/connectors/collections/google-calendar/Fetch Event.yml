info:
  name: Fetch Event
  type: http
  seq: 4

http:
  method: GET
  url: https://www.googleapis.com/calendar/v3/calendars/{{email}}/events/{{event_id}}
  auth: inherit

runtime:
  variables:
    - name: event_id
      value:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
